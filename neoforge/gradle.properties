org.gradle.jvmargs=-Xmx4G -Duser.language=en -Duser.country=US -Dfile.encoding=UTF-8
org.gradle.daemon=false
# Reuse the locally installed JDK 21 instead of provisioning one (the foojay resolver
# plugin lives on plugins.gradle.org, which is unreachable here).
org.gradle.java.installations.paths=G:/java21


## Environment Properties

minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.22)
neo_version=21.1.250
neo_version_range=[21.1.0,)
loader_version_range=[4,)


## Mod Properties

# Must match the String constant in the @Mod annotated class.
mod_id=pigthings
mod_name=PigThings
mod_license=MIT
# Kept in sync with the Forge line, with a suffix marking the NeoForge build.
mod_version=1.6.4-neoforge
mod_group_id=com.liaoliao.flighthelmet
mod_authors=Pig
mod_description=Adds the multifunctional ring to PigThings.
